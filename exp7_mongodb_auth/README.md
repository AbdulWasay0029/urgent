# Exp 7 - MongoDB User Authentication
## How to Run
1. Make sure MongoDB is running on `localhost:27017`
2. Open terminal in this folder
3. Run:
```
npm install
node server.js
```
4. Open browser at `http://localhost:3000`
5. Use Sign Up to register, then Login to access welcome page
